# Copy this file to credentials.yaml and fill in your values
# credentials.yaml is in .gitignore

# HuggingFace (optional - for downloading models)
huggingface:
  token: "your_hf_token_here"  # Optional, for gated models

# Admin User
admin:
  username: "admin"
  password: "admin123"  # Change this!
  email: "admin@awis.com"

# Demo Users
demo_users:
  - username: "hr_manager"
    password: "hr123"
    role: "admin"
  
  - username: "employee"
    password: "emp123"
    role: "employee"